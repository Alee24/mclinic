<IfModule mod_ssl.c>
<VirtualHost *:443>

    ServerName portal.mclinic.co.ke
    ServerAlias www.portal.mclinic.co.ke

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/portal.mclinic.co.ke/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/portal.mclinic.co.ke/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    ProxyPreserveHost On
    ProxyRequests Off

    ## 🔹 API → port 3434
    ProxyPass /api/ http://127.0.0.1:3434/
    ProxyPassReverse /api/ http://127.0.0.1:3434/

    ## 🔹 Frontend / WebSockets → port 3034
    ProxyPass / http://127.0.0.1:3034/
    ProxyPassReverse / http://127.0.0.1:3034/

    ## 🔹 WebSocket support (VERY IMPORTANT)
    RewriteEngine On
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule /(.*) ws://127.0.0.1:3034/$1 [P,L]
    RewriteCond %{HTTP:Upgrade} !=websocket [NC]
    RewriteRule /(.*) http://127.0.0.1:3034/$1 [P,L]

    ErrorLog ${APACHE_LOG_DIR}/portal.mclinic.co.ke-error.log
    CustomLog ${APACHE_LOG_DIR}/portal.mclinic.co.ke-access.log combined

</VirtualHost>
</IfModule>
